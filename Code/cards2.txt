exception Factorial

fun checked_factorial n=
if n < 0 then
	raise Factorial 
else if n=0 then 
	1
else n * checked_factorial(n-1)

fun remove_card(cs: card_list, c: card, e: exn)=
if null cs
then raise e
else if (hd=c) then 
	then (tl cs)
	else (hd=c)::remove_card(tl cs)
